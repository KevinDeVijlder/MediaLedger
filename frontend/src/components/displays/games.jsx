import React from "react";
import { Box, Typography } from "@mui/material";

export default function Games() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h5" sx={{ fontFamily: "Ubuntu" }}>Games</Typography>
      <Typography sx={{ mt: 2 }}>Placeholder page for Games — content to be added.</Typography>
    </Box>
  );
}
