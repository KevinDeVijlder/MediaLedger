import React from "react";
import { Box, Typography } from "@mui/material";

export default function Movies() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h5" sx={{ fontFamily: "Ubuntu" }}>Movies</Typography>
      <Typography sx={{ mt: 2 }}>Placeholder page for Movies — content to be added.</Typography>
    </Box>
  );
}
