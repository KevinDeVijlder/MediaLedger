import React from "react";
import { Box, Typography } from "@mui/material";

export default function TvShows() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h5" sx={{ fontFamily: "Ubuntu" }}>TV Shows</Typography>
      <Typography sx={{ mt: 2 }}>Placeholder page for TV Shows — content to be added.</Typography>
    </Box>
  );
}
